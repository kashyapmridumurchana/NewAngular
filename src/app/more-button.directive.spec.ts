import { MoreButtonDirective } from './more-button.directive';

describe('MoreButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MoreButtonDirective();
    expect(directive).toBeTruthy();
  });
});
